// File-type detection and labels are centralized in the config layer.
// Re-exported here for backwards compatibility with existing imports.
export {
	detectFileType,
	FILE_TYPE_LABELS,
	FILE_TYPE_OPTIONS,
	FILE_TYPE
} from '$lib/config/file-types';
